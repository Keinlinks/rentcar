import { PointNumberPipe } from './point-number.pipe';

describe('PointNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new PointNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
