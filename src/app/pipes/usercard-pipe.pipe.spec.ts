import { UsercardPipePipe } from './usercard-pipe.pipe';

describe('UsercardPipePipe', () => {
  it('create an instance', () => {
    const pipe = new UsercardPipePipe();
    expect(pipe).toBeTruthy();
  });
});
